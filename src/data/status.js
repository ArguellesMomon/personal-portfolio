// "Currently building" badge content — Phase 2 feature (not rendered in Core v1
// yet, per the build priority spec). Kept here so the file is a one-line edit
// once the badge component is built.
const status = {
  active: true,
  projectName: '[PLACEHOLDER: project name]',
};

export default status;
